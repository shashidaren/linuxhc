# Thresholds (percentages unless noted)
CPU_THRESHOLD=80
MEM_THRESHOLD=80
SWAP_THRESHOLD=50
DISK_THRESHOLD=80
INODE_THRESHOLD=90
LOAD_THRESHOLD=5

# Optional additional thresholds
#DISK_ALL_THRESHOLD=85
#LOG_ERR_THRESHOLD=10
#NET_ERR_THRESHOLD=10

# Critical services to verify (cron vs crond auto-handled)
CRITICAL_SERVICES="sshd cron docker"

# Which checks to enable (space-separated). If empty, all discovered checks run.
#ENABLED_CHECKS="system cpu memory disk fs_mounts network net_quality services updates logs users hardware"
